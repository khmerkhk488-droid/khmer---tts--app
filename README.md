# Khmer TTS App

A simple Khmer text-to-speech web app using Flask and edge-tts.

## Run locally

```bash
pip install -r requirements.txt
python app.py
```

## Railway

Use the start command:

```bash
gunicorn app:app
```
